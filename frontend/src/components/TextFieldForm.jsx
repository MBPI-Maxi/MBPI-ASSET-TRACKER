import { TextField } from "@mui/material";

export function RegistrationTextField({ ...props }) {
  return (
    <TextField
      fullWidth
      margin="normal"
      {...props}
    >

    </TextField>
  )
}