import { TextField } from "@mui/material";

export function DefaultTextFieldStyle({ ...props }) {
  return (
    <TextField
      fullWidth
      margin="normal"
      {...props}
    >

    </TextField>
  )
}
